# Backend dependencies for the Page Pilot FastAPI service.
# Pin minor versions for reproducibility; bump intentionally.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.0

# Anthropic SDK — must use this version for claude-sonnet-4-20250514
anthropic==0.40.0

# Settings management
pydantic-settings==2.6.1

# Supabase client (schema only for now — not connected yet)
supabase==2.10.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
